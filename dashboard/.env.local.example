# Copy to .env.local for local dev. Not used in Docker; pass env at runtime.
# Dashboard runs at http://localhost:3000 and talks to API at NEXT_PUBLIC_API_URL.

NEXT_PUBLIC_API_URL=http://localhost:8000
